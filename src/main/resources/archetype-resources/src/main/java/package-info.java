#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
/**
 * Pacote do repositório de "${artifactId}". Contém duas classes
 * apenas: (a) {@link ${package}.Calendario} e
 * (b) {@link ${package}.ProgramaCalendario}.
 * Observe que essa última classe apenas ilustra como um
 * programa em Java pode chamar o código da classe
 * relevante ({@link ${package}.Calendario}).
 *
 * @see ${package}.Calendario
 * @see ${package}.ProgramaCalendario
 */
package ${package};
